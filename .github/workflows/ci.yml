name: CI

on:
    pull_request:
    push:
        branches:
            - main

env:
    PLAYWRIGHT_BASE_URL: http://localhost:8888
    WP_ENV_PORT: 8888
    WP_ENV_CORE_MINIMUM: WordPress/WordPress#6.6
    WP_ENV_CORE_NEXT: https://wordpress.org/wordpress-7.0-RC3.zip

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup pnpm
              uses: pnpm/action-setup@v4
              with:
                  version: 10.20.0

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  cache: pnpm
                  node-version: 20

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Install Playwright browser
              run: pnpm exec playwright install --with-deps chromium

            - name: Lint JavaScript
              run: pnpm run lint:js

            - name: Lint styles
              run: pnpm run lint:css

            - name: Run unit tests
              run: pnpm run test:unit

            - name: Build plugin
              run: pnpm run build

            - name: Reset Docker auth for wp-env images
              run: |
                  mkdir -p "$HOME/.docker"
                  printf '{}\n' > "$HOME/.docker/config.json"
                  docker logout || true

            - name: Start wp-env (WordPress 6.6 minimum)
              env:
                  WP_ENV_CORE: ${{ env.WP_ENV_CORE_MINIMUM }}
              run: pnpm run env:start

            - name: Smoke test (WordPress 6.6 minimum)
              run: pnpm run test:smoke

            - name: Stop wp-env (WordPress 6.6 minimum)
              if: always()
              env:
                  WP_ENV_CORE: ${{ env.WP_ENV_CORE_MINIMUM }}
              run: pnpm run env:stop || true

            - name: Start wp-env (WordPress 7.0 RC)
              env:
                  WP_ENV_CORE: ${{ env.WP_ENV_CORE_NEXT }}
              run: pnpm run env:start

            - name: Upgrade database (WordPress 7.0 RC)
              env:
                  WP_ENV_CORE: ${{ env.WP_ENV_CORE_NEXT }}
              run: pnpm exec wp-env run cli wp core update-db --allow-root

            - name: Smoke test (WordPress 7.0 RC)
              run: pnpm run test:smoke

            - name: Stop wp-env (WordPress 7.0 RC)
              if: always()
              env:
                  WP_ENV_CORE: ${{ env.WP_ENV_CORE_NEXT }}
              run: pnpm run env:stop || true

            - name: Build tested release zip
              run: pnpm run plugin-zip

            - name: Verify tested release zip
              run: test -f "./sbs-typing-effect-block.zip"

            - name: Upload Playwright artifacts
              if: failure()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-artifacts
                  path: |
                      test-results
                      playwright-report
                  if-no-files-found: ignore
